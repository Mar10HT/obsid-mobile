import { View, Text } from 'react-native';

// Placeholder — Fase 2
export default function InventoryScreen() {
  return (
    <View className="flex-1 bg-surface items-center justify-center">
      <Text className="text-foreground font-sans-semibold text-lg">Inventario</Text>
      <Text className="text-on-surface-muted font-sans text-sm mt-1">Fase 2</Text>
    </View>
  );
}
