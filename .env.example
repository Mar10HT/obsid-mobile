# Production API URL (used when EXPO_PUBLIC env var is set and __DEV__ is false)
# In development, the IP is detected automatically from Expo's hostUri.
EXPO_PUBLIC_API_URL=https://your-api-domain.com
